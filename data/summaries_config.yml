# Configuration for creating summary plots across assays.

# --------------------------------------------------------------------------------------
# Define how to create an integrated set of line plots and heat maps that summarizes
# antibody escape alongside other assays (eg, functional effects of mutations).
# --------------------------------------------------------------------------------------

cell_entry_default: &cell_entry_default
  stat: effect  # column to plot
  color_scheme: redblue
  max_at_least: 2.5
  fixed_min: -5
  init_min_value: null
  le_filters:
    effect_std: 2  # remove if std less than this

entry_all_cells:
  min_times_seen: 2  # only include mutations with times_seen >= this
  min_frac_models: 0.75  # only include mutations in >= this fraction of models
  alphabet: [A, C, D, E, F, G, H, I, K, L, M, N, P, Q, R, S, T, V, W, Y]  # amino acids to include
  init_floor_escape_at_zero: true  # initially floor site escape to be >= 0?
  init_site_escape_stat: sum  # initially show site escape as this statistic
  antibody_escape: {}  # show effects on antibody escape, empty dict if no antibodies being shown
  other_phenotypes:
    entry in a23 cells:
      <<: *cell_entry_default
      csv: results/func_effects/averages/293_2-3_entry_func_effects.csv  # CSV w data
    entry in a26 cells:
      <<: *cell_entry_default
      csv: results/func_effects/averages/293_2-6_entry_func_effects.csv
    entry in mix of a23 and a26 cells:
      <<: *cell_entry_default
      csv: results/func_effects/averages/293_mix_entry_func_effects.csv
  mutation_annotation_sliders:
    nt changes to codon:
      init_value: 3
      max_or_min: max
      slider_binding_range_kwargs:
        min: 1
        max: 3
        step: 1

